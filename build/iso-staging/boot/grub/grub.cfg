set default=0
set timeout=3

menuentry "SteamLinkOS Installer (Prototype)" {
    echo "Bootloader de protótipo carregado."
    echo "Este ISO ainda não possui kernel/initramfs live."
    echo "Use o rootfs em /live/rootfs.tar.gz para próximo passo do instalador."
}
